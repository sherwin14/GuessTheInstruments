<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <Button

        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/game_image"
        android:background="@color/colorAccent"
        android:text="Play with Images"
        android:textColor="@color/colorLight"
        android:layout_margin="25dp"
        android:drawableRight="@drawable/ic_portrait"
        android:paddingLeft="30dip"
        android:paddingRight="26dip"

        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/game_sound"
        android:background="@color/colorPrimary"
        android:text="Play with Music"
        android:textColor="@color/colorLight"
        android:layout_below="@+id/game_image"
        android:layout_alignLeft="@+id/game_image"
        android:layout_alignStart="@+id/game_image"
        android:layout_alignRight="@+id/game_image"
        android:layout_alignEnd="@+id/game_image"
        android:drawableRight="@drawable/ic_music_video"
        android:paddingLeft="30dip" android:paddingRight="26dip"
        />
</RelativeLayout>